describe('Array', () => {
    it('Should return an array', () => {
        const names: string[] = ['a', 'b', 'c'];
        const values: number[] = [1, 2, 3, 4, 5];

        console.info(names);
        console.info(values)
    })
});