describe('Data Type', () => {
    it('Should must declare', () => {
        const name: string = "Fadhil Rahman";
        const balance: number = 10;
        const isVip: boolean = true;

        console.log(name);
        console.log(balance);
        console.log(isVip);
    })
})